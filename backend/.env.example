PORT=3001
FRONTEND_URL=http://localhost:5173

# API keys are stored in SQLite via Settings tab — no env vars required in normal use.
# Set these to override DB values (useful for CI/Docker/cloud deploy):
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
NEWS_API_KEY=
